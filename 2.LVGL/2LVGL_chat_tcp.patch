diff --git a/2.LVGL/tcp.c b/2.LVGL/tcp.c
--- a/2.LVGL/tcp.c
+++ b/2.LVGL/tcp.c
@@
 #include <sys/socket.h>
 #include <netinet/in.h>
 #include <arpa/inet.h>
+#include "myhead.h"

 /*
     TCP客户端通信
 */
 int tcpsock;
+char chatip[20]={0};
+unsigned short chatport=0;
+lv_obj_t *tcp_list=NULL;

 void *recv_fun(void *arg)
 {
@@
         if(strcmp(p1,"getlist")==0) //服务器发过来的是在线客户端列表
         {
             char *p2;
+            if(tcp_list!=NULL && lv_obj_is_valid(tcp_list))
+                lv_obj_clean(tcp_list);
             while((p2=strtok(NULL,"#"))!=NULL)
             {
                 printf("在线的好友：%s\n",p2);
                 //stp接入
+                if(tcp_list!=NULL && lv_obj_is_valid(tcp_list))
+                {
+                    lv_obj_t *bt=lv_list_add_btn(tcp_list,NULL,p2);
+                    extern void fri_chat_cb(lv_event_t * e);
+                    lv_obj_add_event_cb(bt,fri_chat_cb,LV_EVENT_CLICKED,NULL);
+                }
             }
         }
@@
             char *p3=strtok(NULL,"#"); //真实的信息
             printf("%s发给我的信息：%s\n",p2,p3);
             //stp接入
+            if(chatmsg!=NULL && lv_obj_is_valid(chatmsg))
+            {
+                char allmsg[2048]={0};
+                sprintf(allmsg,"%s\n%s\n",p2,p3);
+                lv_textarea_add_text(chatmsg,allmsg);
+            }
+            else
+                chatmsg=NULL;
         }
     }
 }
diff --git a/2.LVGL/main.c b/2.LVGL/main.c
--- a/2.LVGL/main.c
+++ b/2.LVGL/main.c
@@
     if(strlen(ip) == 0 || strlen(port) == 0)
         return;

     //stp接入
+    extern char chatip[20];
+    extern unsigned short chatport;
+    bzero(chatip,20);
+    strcpy(chatip,ip);
+    chatport=atoi(port);
     show_chat();
     lv_obj_del(oldwin);
 }
diff --git a/2.LVGL/friend.c b/2.LVGL/friend.c
--- a/2.LVGL/friend.c
+++ b/2.LVGL/friend.c
@@
 void fri_chat_cb(lv_event_t * e)
 {
     lv_obj_t *oldwin = setwin;
     //stp接入
+    lv_obj_t *bt=lv_event_get_target(e);
+    lv_obj_t *list=lv_obj_get_parent(bt);
+    const char *info=lv_list_get_btn_text(list,bt);
+    char buf[50]={0};
+    strcpy(buf,info);
+    char *ip=strtok(buf,"@");
+    char *port=strtok(NULL,"@");
+    extern char chatip[20];
+    extern unsigned short chatport;
+    if(ip!=NULL && port!=NULL)
+    {
+        bzero(chatip,20);
+        strcpy(chatip,ip);
+        chatport=atoi(port);
+    }
     show_chat();
     lv_obj_del(oldwin);
 }
@@
     lv_obj_set_style_radius(list, 0, 0);
     lv_obj_set_style_border_width(list, 0, 0);
     //stp接入
+    extern lv_obj_t *tcp_list;
+    tcp_list=list;
+    tcp_getlist();
     // 获取好友之后使用lv_list_add_btn创建好友按钮
     // 好友按钮点击事件使用fri_chat_cb
diff --git a/2.LVGL/chat.c b/2.LVGL/chat.c
--- a/2.LVGL/chat.c
+++ b/2.LVGL/chat.c
@@
     const char *msg = lv_textarea_get_text(ta1);
     if(strlen(msg) == 0)
         return;
-    tcp_test((char *)msg);
+    extern char chatip[20];
+    extern unsigned short chatport;
+    if(strlen(chatip)==0 || chatport==0)
+        return;
+    tcp_chat(chatip,chatport,(char *)msg);

 }
@@
     lv_obj_set_style_radius(list, 0, 0);
     lv_obj_set_style_border_width(list, 0, 0);
     //stp接入
+    extern lv_obj_t *tcp_list;
+    tcp_list=list;
+    tcp_getlist();
     //获取好友之后使用lv_list_add_btn创建好友按钮
     //好友按钮点击事件使用fri_chat_cb
@@
     lv_obj_add_style(lb1,mystyle,0);
     lv_label_set_text(lb1, "chat with friend");
     //stp接入
+    extern char chatip[20];
+    extern unsigned short chatport;
+    if(strlen(chatip)!=0 && chatport!=0)
+    {
+        char info[100]={0};
+        sprintf(info,"%s:%hu",chatip,chatport);
+        lv_label_set_text(lb1,info);
+    }

     // 用来显示聊天界面的额外功能 里面有一个文本框 用来搜索聊天记录 有一个按钮用来清空聊天记录 有一个按钮 用来删除好友 
